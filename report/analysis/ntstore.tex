
As long as this benchmark uses \textbf{non-temporal stores} (\textit{ie} does not uses cache), the bandwidths computed does not depend on the L* caches, and the result for all runs is actually the bandwidth of the \cleanstyle{DRAM} of my computer - which is about 28GiB/s.